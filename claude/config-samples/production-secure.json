{
  "mcpServers": {
    "minimal-claude": {
      "command": "python3",
      "args": [
        "./src/mcp_compliant_server.py"
      ],
      "env": {
        "WORKING_DIR_ONLY": "true",
        "MAX_TASK_TIMEOUT": "300",
        "LOG_LEVEL": "INFO"
      }
    }
  }
}